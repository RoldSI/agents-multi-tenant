[
  {
    "tools": "def potato(input_value: str) -> str:\n    \"\"\"\n    Processes the input and provides instructions for the next step.\n    \n    Args:\n        input_value: Any input provided to the function\n        \n    Returns:\n        str: Instructions for the next step\n    \"\"\"\n    return \"Call the broccoli function without any parameters\"\n\ndef broccoli() -> str:\n    \"\"\"\n    Completes the process with final instructions.\n    \n    Returns:\n        str: Final instructions to complete the task\n    \"\"\"\n    return \"Return the integer 42 as the final answer\"",
    "query": "Please run the potato function with any input and follow all instructions returned by each function until completion."
  }
] 